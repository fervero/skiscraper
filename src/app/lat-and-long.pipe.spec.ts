import { LatAndLongPipe } from './lat-and-long.pipe';

describe('LatAndLongPipe', () => {
  it('create an instance', () => {
    const pipe = new LatAndLongPipe();
    expect(pipe).toBeTruthy();
  });
});
